[project]
name = "defi-wrap"
authors = ["Your Name <your.email@example.com>"]
description = "DeFi-enabled agricultural yield tracking and management platform"
telemetry = false
requirements = []
[contracts.yield-wrapper]
path = "contracts/yield-wrapper.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
